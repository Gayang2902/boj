#include <stdio.h>
#include <string.h>

int main(void)
{
    int T, N;
    int i;
    int cache[2][100001];

    scanf("%d", &T);
    for (i = 0; i < T; i++) {
        scanf("%d", &N);
        memset(cache, 0, sizeof(cache));
        
        
    }
}